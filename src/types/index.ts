type HeroTypes = {
  hi: string;
  name: string;
  strong: string;
  desc: string;
};

type NavMenuTypes = { title: string; href: string; description: string }[];

export type { HeroTypes, NavMenuTypes };
